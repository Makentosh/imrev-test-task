import { LoginForm } from '../../components';

const LoginPage = () => {
  return (
      <div className={'min-h-dvh flex align-center justify-center'}>
        <LoginForm/>
      </div>
  );
};

export default LoginPage;
